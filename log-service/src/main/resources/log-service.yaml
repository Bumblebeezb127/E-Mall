emall:
  log:
    root: ${EMALL_LOG_ROOT:logs}
    max-file-size-mb: 50
    max-tail-lines: 5000

management:
  endpoints:
    web:
      exposure:
        include: '*'
    health:
      show-details: always
