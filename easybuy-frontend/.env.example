# E-Mall 前端环境变量示例
# 复制此文件为 .env.development / .env.production 并按需修改

# 后端 API 基础 URL
# - 开发环境 (Vite dev server): 留空, 走 vite.config.js 的 /api 代理
# - 生产环境 (部署到服务器):  填反向代理后的地址, 如 '/api' 或 'https://api.example.com'
#
# 重要: 不要写 'http://localhost:9000' 这种硬编码绝对 URL,
#       在其他人机器上部署时会 403/404
VITE_API_BASE_URL=
